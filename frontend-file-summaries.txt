{
  "frontend/src/App.tsx": "Main app with TanStack Router; RootComponent now wraps Header + Outlet + Footer in a flex-col min-h-screen layout so the Footer appears on every page/route.",
  "frontend/src/hooks/useQueries.ts": "Comprehensive React Query hooks for all backend operations; adds the previously missing `useHasUserLikedReply` (wraps `hasUserLikedReplyForCaller`) and `useGetVideoRepliesByUser` hooks, plus `useGetVisitorStats` and `useRecordVisit` for the admin visitor counters, with all other existing hooks intact.",
  "frontend/src/components/Header.tsx": "Sticky navigation header now fetches the authenticated user's profile and displays their uploaded avatar as a small circular Avatar in the account button, falling back to the User icon when no avatar is set.",
  "frontend/src/components/Branding.tsx": "Simple branding component displaying \"15sec\" text with animated blinking red dot.",
  "frontend/src/components/LoginPrompt.tsx": "Full-screen login prompt card shown to unauthenticated users with updated text \"Share your world in 15 seconds. Sign in to get started.\" and Internet Identity sign-in button.",
  "frontend/src/components/UsernameSetupModal.tsx": "Modal for first-time username setup; refactored to use `useCheckUsernameAvailability` as a mutation (not a query), managing availability state locally with debounced async checks.",
  "frontend/src/components/NotificationPanel.tsx": "Dropdown notification panel that now uses `max-w-[calc(100vw-1rem)]` alongside `w-80` so it never overflows the screen on narrow mobile viewports while remaining right-aligned under the bell icon.",
  "frontend/src/components/VideoPostCard.tsx": "Video post card now fetches the creator's `UserProfile` via `useGetUserProfile` and displays their uploaded avatar (converted from Uint8Array to blob URL) in the avatar slot, falling back to the username initial.",
  "frontend/src/components/VideoReplyCard.tsx": "Video reply card now fetches the creator's `UserProfile` via `useGetUserProfile` and displays their uploaded avatar (converted from Uint8Array to blob URL) in the avatar slot, falling back to the username initial.",
  "frontend/src/components/ShareDialog.tsx": "Dialog for sharing videos or replies with copy-to-clipboard and internal share options; now accepts an optional replyId prop and calls shareReply or sharePost accordingly, with the share URL including a reply anchor when replyId is provided.",
  "frontend/src/components/ReplyDialog.tsx": "Dialog wrapper for video reply creation; uses `w-[calc(100vw-2rem)] max-w-[calc(100vw-2rem)]` on mobile to stay fully within the viewport, switching to `sm:max-w-2xl` on larger screens, with `overflow-x-hidden` and right padding on the header to avoid the close button overlap.",
  "frontend/src/components/VideoPostCreator.tsx": "Form component for creating video posts with title input, upload progress tracking, and actor availability check to prevent \"actor not available\" errors.",
  "frontend/src/components/VideoReplyCreator.tsx": "Component for creating video replies with upload/record modes; all containers use `w-full min-w-0` and `overflow-hidden` to prevent any child from expanding beyond the modal width on mobile.",
  "frontend/src/components/VideoUploader.tsx": "File input component for uploading videos with automatic trimming of videos exceeding maxDuration to the first maxDuration seconds, using canvas-based frame extraction and MediaRecorder to create trimmed video files.",
  "frontend/src/components/VideoRecorder.tsx": "In-browser video recorder using camera hook; replaced the `minHeight: 300px` + `aspectRatio: 16/9` (which forced ~533px width) with a `w-full` + `aspectRatio: 4/3` container so the preview scales responsively within any parent width, and all button containers use `w-full min-w-0` with `flex-1 min-w-0` children.",
  "frontend/src/components/FollowButton.tsx": "Follow/unfollow button component that accepts `targetPrincipal` as a `string` (updated from `Principal`) and uses the string-based `useIsFollowing`, `useFollowUser`, and `useUnfollowUser` hooks.",
  "frontend/src/pages/HomePage.tsx": "Home feed page displaying all video posts sorted by timestamp in reverse chronological order.",
  "frontend/src/pages/CreatePage.tsx": "Video creation page with mode selection (upload/record) and post creation flow with updated heading text \"Share your world in 15 seconds.\" using onSuccess callback prop.",
  "frontend/src/pages/AccountPage.tsx": "User account page now fetches and displays Followers and Following counters (via `useGetFollowersAndFollowingCounts`) prominently below the username in the profile card.",
  "frontend/src/pages/ProfilePage.tsx": "Public user profile page; fixed to use renamed `useGetFollowersCount`/`useGetFollowingCount` hooks (replacing `useGetFollowerCounts`), pass string principals to all hooks, and pass `targetPrincipal` as a string to `FollowButton`.",
  "frontend/src/pages/VideoDetailPage.tsx": "Video detail page; fixed to convert the `videoId` string param to `bigint | null` before passing to `useGetVideoPostById`, `useGetRepliesForPostWithPagination`, and `useGetRepliesCountForPost` which now all accept `bigint | null`.",
  "frontend/src/index.css": "Global styles with minimalist dark/light mode color system using HSL values, red destructive accent color for branding and interactions.",
  "frontend/tailwind.config.js": "Tailwind configuration with HSL-based color tokens, Inter font family, and custom radius values for minimalist modern design.",
  "frontend/src/pages/AdminDashboardPage.tsx": "Admin dashboard page with user management table and stats; adds four new visitor counter cards (Daily, Weekly, Monthly, All-Time Visitors) below the existing Active Accounts and Total Users cards, and records the authenticated admin's visit via `useRecordVisit` on page load.",
  "frontend/src/components/CookieConsent.tsx": "Cookie consent pop-up component that displays on first visit with clear text explaining cookie usage, Accept button that stores consent in localStorage and dismisses the pop-up permanently.",
  "frontend/src/components/Footer.tsx": "New simple footer component rendering \"© 2026. All rights reserved.\" with border-top styling, used sitewide via the root layout in App.tsx."
}